{
  "version": 2,
  "buildCommand": "cd hospital_crm/frontend && npm install && npm run build && cd ../.. && node -e \"const fs=require('fs'); fs.cpSync('hospital_crm/backend/app', 'api/app', {recursive: true});\"",
  "outputDirectory": "hospital_crm/frontend/dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.py"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
